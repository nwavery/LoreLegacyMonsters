# Writing-agent proposal (iter 6)

- completionsUrl: http://127.0.0.1:11434/v1/chat/completions
- model: llama3.2:latest
- utc: 2026-05-13T18:30:57.6795634Z

---

Here's an overview of what this code snippet does:

**LoreLegacyMonsters.Dialog.Llm**

This C# namespace contains a class `NpcLlmScenarioEvaluator` that is responsible for evaluating NPC (Non-Player Character) scenarios in a game.

**TryEvaluateHud**

The `TryEvaluateHud` method takes two inputs:

1. `shapedHud`: a string representing the HTML user interface (HUD) of an NPC.
2. `scenario`: an instance of `NpcLlmScenarioRecord`, which contains information about the NPC scenario, such as policies and restrictions.

The method returns `true` if the HUD passes all validation checks, and `false` otherwise.

**Validation Checks**

The method performs several validation checks on the HUD:

1. **Coach Hud Leak**: It checks if there are any coach-specific markers in the HUD (e.g., `[[/command:]], `[[[command]]]`) that could indicate a language model leak.
2. **Global Policies**: It applies global policies to the HUD, such as blocking coaching-style expressions and ensuring that stage directions are rewritten as plain speech.
3. **Substring Pipe**: It checks if there are any forbidden substrings in the HUD (e.g., " choose your response").
4. **Regex Pipe**: It applies regex patterns to the HUD, including checking for matches with specific keywords (e.g., "large language model").
5. **Max Paragraphs**: It ensures that the Hudson is well-formed by limiting the number of paragraphs.

If any of these checks fail, the method returns `false`.

**LooksLikeMlWeightsLeak**

This method is a simple utility function that checks if a given string contains certain keywords related to machine learning "weights" or "parameters".

**Run Summary**

The code also includes an excerpt from a JSON file called `run_summary.json`, which provides information about the evaluation process, such as:

* The number of scenarios attempted
* The number of failures (LLM and evaluative)
* Failure details for each failing scenario

Overall, this code is designed to ensure that NPC interactions in a game are coherent and follow established guidelines, while also detecting potential machine learning "leaks" or errors.
