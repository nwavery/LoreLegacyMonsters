# Writing-agent proposal (iter 9)

- completionsUrl: http://127.0.0.1:11434/v1/chat/completions
- model: llama3.2:latest
- utc: 2026-05-13T19:06:56.9421449Z

---

This is a code snippet from a dialogue system for a game or simulation, specifically designed to evaluate and filter the output of a language model (LLM) used in a chat interface.

The code defines two main functions:

1. `TryEvaluateHud`: This function takes three inputs:
	* `shapedHud`: The input HUD text.
	* `scenario`: An LLM scenario record object, which likely contains settings or rules to evaluate the input text against.
	* `failureReason` (output): A string indicating why the input text failed the evaluation.

The function's purpose is to determine if the input HUD text passes various filters and rules based on the scenario settings. These filters might involve checking for specific keywords, phrases, or patterns that are deemed unacceptable for an NPC's speech.

To illustrate how this code works:

1. The `TryEvaluateHud` function starts by checking if any specific "global policies" are applied to the input text (`hed`). If so, it returns a failure message and stops executing further checks.
2. Next, it uses `NpcLlmResponseFilter.TryDetectCoachHudLeak(hud)` to detect whether there's a coach-leak pattern in the HUD text (i.e., whether someone attempted to inject external influence or scripting into the model). If so, it returns an error message and stops.
3. It then checks if the input text contains certain problematic expressions or phrases, such as those related to exam-style questioning, screenplay-style stage directions, or specific language models' capabilities. These checks are case-sensitive (using `StringComparison.OrdinalIgnoreCase`) to minimize false positives.
4. If any of these checks fail, the function returns a failure reason and stops executing further rules.

The purpose of this code is likely to ensure that NPCs in the game or simulation generate responses that are:

* Immersive and engaging for players
* Consistent with the story Bible (a guidebook outlining the narrative expectations)
* Free from problematic language patterns or coaching-style messages

This filter helps maintain a coherent and believable tone across all conversations within the game.
