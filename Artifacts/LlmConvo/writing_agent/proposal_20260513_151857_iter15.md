# Writing-agent proposal (iter 15)

- completionsUrl: http://127.0.0.1:11434/v1/chat/completions
- model: llama3.2:latest
- utc: 2026-05-13T20:18:57.8217494Z

---

This code appears to be part of a larger system for generating and evaluating dialogue in a role-playing game (RPG) world, specifically with the use of a language model like LLaMA.

Here's a high-level overview of what the code does:

1. **NpcLlmScenarioEvaluator**: This class contains methods for evaluating HUD strings (heads-up display strings) generated by NPCs (non-player characters) in the game world.
2. **TryEvaluateHud**: This method takes a shaped HUD string and an NPC LLM scenario record as input, and returns `true` if the HUD string passes all evaluation criteria, or `false` otherwise. It checks for various types of errors, such as:
	* Exam-style HUDs that contain `Choose your response`.
	* Generic wiki/headquarters voice.
	* Stage directions with emphasis.
	* Parenthesized stage direction.
	* Certain phrases related to large language models/computational minds.
3. **PassesGlobalHudPolicy**: This method is used in the `TryEvaluateHud` method to check if an input HUD string passes a set of global evaluation policies for ensuring that it doesn't slip automated coach-leak checks or contain diegetic NPC HUD prose.

Some notes on the code:

* The `NpcLlmScenarioRecord` class is not shown in this excerpt, but it likely contains information about the NPC's behavior and response patterns.
* The `tryMaxParagraphGuard` method is called with a maximum paragraph count of `$scenario.maxParagraphs`. It appears to check if the HUD string exceeds this limit and provides an error message if so.
* The code uses regex patterns to detect certain errors or phrases in the input HUD string. These patterns are likely defined in a separate configuration file or resource.

To write similar code, you would need to:

1. Create a class for evaluating HUD strings (e.g., `HUDEvaluator`).
2. Define methods for checking various types of errors in the HUD string, such as those mentioned above.
3. Use regex patterns or other techniques to detect these errors.
4. Ensure that your implementation adheres to the story bible cast notes and tone match guidelines provided.

Keep in mind that this code appears to be focused on a specific aspect of NPC behavior and dialogue in an RPG world, so the breadth and complexity of this project may be higher than what you're used to.
