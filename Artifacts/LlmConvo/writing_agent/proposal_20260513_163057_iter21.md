# Writing-agent proposal (iter 21)

- completionsUrl: http://127.0.0.1:11434/v1/chat/completions
- model: llama3.2:latest
- utc: 2026-05-13T21:30:57.1268480Z

---

Based on the text, it appears that there is a logging mechanism in place to track conversations between the Lore Legacy Monsters game and its players using a language model (LLM). The logs are generated from the `NpcLlmScenarioEvaluator.cs` class.

The logs contain information such as:

* The ID of the scenario being evaluated
* The input shaped HUD (User Interface Data)
* The output response from the LLM
* Various error messages or failure reasons
* Statistics on the performance of the LLM, including the number of scenarios attempted and failed

One of the error messages in particular catches my attention:

`"Hud contains `please respond with` coaching."`

This suggests that there may be issues with the design of the HUD (User Interface Data) or the way it is being used by the LLM. It's possible that the HUD is too coachy or contains prompts that are not suitable for use in the game.

Furthermore, I notice that the logs mention a failed test for a specific NPC's HUD policy:

`"HUD reads like an encyclopedia entry (`â¦is a location in theâ¦`)."`

This suggests that there may be issues with how certain NPCs' dialogue is written or structured.

To improve the performance of the LLM and reduce errors, it might be useful to review and refine the design of the HUDs for each NPC. This could involve ensuring that the prompts are clear and concise, and that they do not contain coaching language or formatting issues.
